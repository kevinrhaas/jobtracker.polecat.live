{
  "_doc": "Promotion-pipeline config (dev → qa → main). DATA file, like polecat-platform's .github/steward/focus.json — editing it is a sanctioned direct commit to main (sessions: pull → edit → push; Manager: contents-API PUT with sha compare-and-swap). Changes take effect on the next hourly tick of promote-to-qa.yml. Fields: promoteToQa.enabled turns the scheduled dev→qa promotion on/off entirely; paused is a soft hold (schedule keeps ticking, does nothing); everyHours is the cadence; offset anchors which UTC hour the cadence counts from; window [start,end) limits promotions to those UTC hours. Manual dispatches of promote-to-qa.yml ignore the schedule (but respect nothing else here). qa→prod promotion is promote-to-prod.yml, dispatch-only, and refuses while the latest qa promotion run is red unless requireGreenQaForProd is false or the dispatch passes force.",
  "promoteToQa": {
    "enabled": true,
    "paused": false,
    "everyHours": 24,
    "offset": 7,
    "window": [
      0,
      24
    ]
  },
  "gates": {
    "devGate": "node .github/validate.mjs + node .github/smoke-test.mjs (ci.yml on PRs into dev and pushes to dev)",
    "qaSuite": "node .github/smoke-test.mjs against the STAGED /qa/ form (promote-to-qa.yml)",
    "qaSuiteTimeoutMinutes": 30
  },
  "requireGreenQaForProd": true
}
